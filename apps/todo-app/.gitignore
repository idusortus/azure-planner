# Environment files
.env
.env.local
.env.*.local

# Azure configuration with sensitive data
azure-config.json
azure-static-web-config.json
azure-keyvault-config.json
azure-budget-config.json

# Manual notes (may contain secrets)
SETUP_NOTES.md

# User secrets
secrets.json

# Database connection strings
connection-strings.txt
*.connection.json

# Deployment tokens
deployment-token.txt
